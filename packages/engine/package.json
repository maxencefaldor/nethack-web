{
  "name": "@nethack-web/engine",
  "version": "0.0.0",
  "private": true,
  "license": "NGPL",
  "description": "The official NetHack engine compiled to WebAssembly, hosted in a worker behind the protocol contract.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./worker": "./src/worker.ts"
  },
  "scripts": {
    "build:wasm": "bash scripts/build.sh",
    "build": "tsc -b",
    "test": "vitest run"
  },
  "dependencies": {
    "@nethack-web/protocol": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
